// Firebase client is disabled for the marketing/landing site.
// This stub prevents bundling Firebase client libraries while keeping imports working.

const app = null;
const analytics = null;

const auth = {
    // Minimal placeholder values used by imports across the codebase.
    currentUser: null
};

const db = null;

export { app, analytics, auth, db };
